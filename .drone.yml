kind: pipeline 

steps:
- name: Servers
  image: alpine
  commands:
    - ls
    - cd ventas/target
    - ls
    - apk add curl     
    - curl -v -u admin:123 -T ventas-0.0.1-SNAPSHOT.war 'http://cmbhhduxoe61kd3z5xyaps.webrelay.io/manager/text/deploy?path=/my-app-path&update=true'